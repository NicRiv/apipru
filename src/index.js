import { config as dotenv } from "dotenv"

import app from "./app"
import "./database"

dotenv()
const PORT = process.env.PORT
// app.listen(PORT)
// console.log(`running on port: ${PORT}`)
console.log('jjjols')